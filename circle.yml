machine:
  node:
    version: 6.9.1

checkout:
  post:
    - "[[ ! -s \"$(git rev-parse --git-dir)/shallow\" ]] || git fetch --unshallow"
    - git checkout -f $CIRCLE_BRANCH

dependencies:
  post:
    - npm run compile

test:
  override:
    - npm run vile
